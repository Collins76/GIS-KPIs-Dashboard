---
description: Build the GIS KPI Dashboard project.
---

1. Install dependencies:
   ```bash
   npm install
   ```
2. Typecheck the project (optional but recommended):
   ```bash
   npm run typecheck
   ```
3. Build the Next.js application:
   ```bash
   npm run build
   ```
